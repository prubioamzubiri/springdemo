name: ci

on:
  push:
    branches:
      - 'Prueba--pushdocker'

jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Set up JDK 13
        uses: actions/setup-java@v1
        with:
          java-version: 1.13
      - name: Maven Package without test
        run: mvn -B clean package -DskipTests
      - name: pwd 
        run: pwd
      - name: ls -l
        run: ls -l /home/runner/work/springdemo/springdemo/target
      - name: Login to DockerHub
        uses: docker/login-action@v1 
        with:
          username: ${{secrets.dockerhub_username}}
          password: ${{secrets.dockerhub_password}}
      - name: Docker Build
        run: docker build . --tag springdemo
      - name: Docker push
        run: docker push springdemo